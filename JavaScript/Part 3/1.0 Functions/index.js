// functions

function hello() {
  return 'Hello'; // use return so you can store it
}

fun = hello();

console.log(fun);

function add(num1, num2) { // num1, num2 are parameters
  return num1 + num2;
}

sum = add(8, 9);
console.log(sum);

function checker(a, b) {
  return a > b; // this will return a boolean
}

chk = console.log(checker(2,9));
