// Type conversation

// User input is a string data type sometimes you need to convert

//typeof is used to know type

let age = Number(window.prompt('Enter your age')); // this easily convert it to a Number


console.log(typeof age);
console.log(`Your age is ${age}`);
