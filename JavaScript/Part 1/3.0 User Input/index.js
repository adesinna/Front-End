// We can get user input

userName = window.prompt('Enter your username:')

console.log(`Your username is ${userName}`); // this is the equivalent of f strings in python note it is ``n used not the normal string quotation
