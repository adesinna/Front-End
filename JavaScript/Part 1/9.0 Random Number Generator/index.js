// There is a random method in the math module

// Math.random() this generate a random (0, 1) 0 < x < 1

let x = Math.floor(Math.random() * 6); // this will generate a random number between 0 and 5

console.log(x+1); // [1, 6]
